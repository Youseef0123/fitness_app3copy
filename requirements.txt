# requirements.txt
# Use version ranges instead of exact versions
protobuf>=3.20.3,<5.0.0
tensorflow==2.17.0
tensorboard==2.17.1
mediapipe==0.10.14
# Add other dependencies with more flexible version constraints
numpy>=1.20,<2.0
Flask==3.0.3
# ... other dependencies